# Full-pipeline dependencies (training + auditing; Tier 4 in REPRODUCIBILITY.md).
# Cache-only aggregation (`qed-rf bench` / `audit` from artifacts/runs_*.json)
# needs just numpy + matplotlib; the test suite needs just numpy.
torch>=2.1
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
matplotlib>=3.7
# deepmimo>=4.0   # only to regenerate channels from scratch; the substrate's
#                 # cached .npy channels make it unnecessary
#
# Note: no lock file is shipped because the exact versions used for the cached
# artifacts were not snapshotted at run time. Capture one on the lab machine
# before the next full rerun:  pip freeze > requirements-lock.txt
